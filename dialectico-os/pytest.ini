[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning

[tool:pytest]
# Configuración de coverage (opcional)
# coverage = dialectico-os
# coverage-report = term-missing
